{%- assign conda = page.conda %}
1. Create your Conda environment:
    * Load Conda.
    {%- if page.cluster %}{%- if page.cluster == "Atlas" %}
        * Atlas: `module load miniconda3`
    {%- else %} 
        * Ceres: `module load miniconda`
    {%- endif %}{%- else %} 
        * Atlas: `module load miniconda3`
        * Ceres: `module load miniconda`
    {%- endif %}
    {%- if conda.first %}
    * Create and load your new Conda environment for this workbook.
        ```bash
conda env create --prefix /90daydata/scinet/$USER/envs/{{ envtitle }}
source activate /90daydata/scinet/$USER/envs/{{ envtitle }}
conda install{% for _c in conda %} {{ _c }}{% endfor %}
```  
    {%- else %}
    * Download the environment yml file for this workbook:
        ```bash
wget {{ file_path }}/{{ conda }}.yml
```
    * Create and load your new Conda environment for this workbook.
        ```bash
conda env create --prefix /90daydata/shared/$USER/envs/{{ envtitle }} -f {{ conda }}.yml
source activate /90daydata/shared/$USER/envs/{{ envtitle }}
```  
{%- endif %}