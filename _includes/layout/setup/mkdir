{% assign mkdir = include.mkdir %}
{%- if mkdir.first %}
{%- assign mfolder = mkdir.first %}
{%- assign shiftedarr = mkdir | shift %}
{%- capture additionalcode %}{% for _line in shiftedarr %}
{{ _line}}
{% endfor %}{% endcapture %}
{%- else %}
{%- assign mfolder = mkdir %}
{%- assign additionalcode = "" %}
**Create a workshop working directory** by running the following commands. Note: you do not have to edit the commands with your username as it will be determined by the $USER variable.
```
mkdir -p /90daydata/shared/$USER/{{ mforlder }}
cd /90daydata/shared/$USER/{{ mfolder }}{{ additionalcode }}
```