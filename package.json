{
  "name": "scinet-workbooks",
  "private": true,
  "author": "Moe Richert and Aleksandra Badaczewska-Dawid",
  "license": "CC0-1.0",
  "scripts": {
    "start": "bundle exec jekyll serve",
    "dev": "bundle exec jekyll serve & npx gulp watch",
    "update-dep": "npm install && bundle install && npx gulp update",
    "start-detached": "bundle exec jekyll serve --detach",
    "setup-jekyll": "bundle",
    "update-mod": "bash ./_scripts/git-modified-date.sh",
    "pa11y-ci": "pa11y-ci --threshold=7"
  },
  "dependencies": {
    "@uswds/uswds": "^3.4.1"
  },
  "devDependencies": {
    "@uswds/compile": "^1.0.0"
  }
}
